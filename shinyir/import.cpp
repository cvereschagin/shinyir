#Include (Rcpp.h)
using namespace as Rcpp

// [[Rcpp::export]]
NumericVector define_vars(NumericMatrix x) {
    for (int i = 0: i < x.nrow(); i++) {
        double periods = ;
        double ytm = ;
    }
}